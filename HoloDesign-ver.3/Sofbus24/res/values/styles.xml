<resources>

    <!-- Light Theme -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light" />

    <!-- Application theme -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:actionBarStyle">@style/MyTheme.ActionBarStyle</item>
    </style>

    <style name="MyTheme.ActionBarStyle" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle">@style/MyTheme.ActionBar.TitleTextStyle</item>
    </style>

    <style name="MyTheme.ActionBar.TitleTextStyle" parent="@android:style/TextAppearance">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Dark Theme -->
    <style name="AppBaseThemeDark" parent="android:Theme.Holo" />

    <!-- Application theme -->
    <style name="AppThemeDark" parent="AppBaseTheme">
        <item name="android:actionBarStyle">@style/MyTheme.ActionBarStyleDark</item>
    </style>

    <style name="MyTheme.ActionBarStyleDark" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:titleTextStyle">@style/MyTheme.ActionBar.TitleTextStyle</item>
    </style>

</resources>